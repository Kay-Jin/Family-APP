name: family_mobile
description: Family app mobile client scaffold.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  intl: ^0.20.2
  record: 6.2.0
  path_provider: ^2.1.4
  just_audio: ^0.10.5
  audio_session: ^0.2.3
  supabase_flutter: ^2.10.0
  gotrue: ^2.19.0
  fluwx: ^5.7.5
  path: ^1.9.1
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  flutter_timezone: ^3.0.1
  cryptography: ^2.7.0
  local_auth: ^3.0.0
  local_auth_android: ^2.0.0
  local_auth_darwin: ^2.0.0
  local_auth_platform_interface: ^1.1.0
  local_auth_windows: ^2.0.0
  device_info_plus: ^11.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
